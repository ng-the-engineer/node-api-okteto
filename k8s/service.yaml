apiVersion: v1
kind: Service
metadata:
  name: node-csv-api
spec:
  type: LoadBalancer
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 5055
  selector:
    app: node-csv-api
