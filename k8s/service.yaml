apiVersion: v1
kind: Service
metadata:
  name: node-csv-api
  labels:
    app: node-csv-api
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  selector:
    app: node-csv-api
